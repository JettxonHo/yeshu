# V2-a 生产部署记录(2026-08-02)

> 本次部署把 V2-a 交互状态机(PR #2)从干净的 main 推上生产,并完成 HTTP smoke + 飞书原生全验证。本文件只记录脱敏信息:不含完整生产 URL、Token、Secret、App ID、Open ID、Project / Field node ID、AccessKey 与本机绝对路径。

## 部署基线

- 部署日期:2026-08-02(本地);FC 更新时间 `2026-08-01T20:41:11Z`(UTC)
- Git commit:`eb20515c6db0b2c5cee1db85e45bfc2c24055f77`(main,PR #2 Squash Merge)
- 构建来源:干净 main(工作区仅未跟踪审计目录;本地 main 与 origin/main 精确匹配)
- main CI run:`30714704443`(push 事件;worker success / python success;7 files / 168 passed)

## 构建与产物

- 构建环境:Node v20.20.2(项目本地版本文件锁定,与 FC `nodejs20` 及 CI 对齐;系统默认仍为 Node 24)
- 产物:`worker/dist/index.js`(esbuild 单文件 CJS)
- 产物 SHA-256:`e86567b575b2419299c423b7b03fb3221cf5a01ddbb0ba6427a48a9449a12fba`
- 产物大小:84622 字节
- 构建时间:`2026-08-01T20:20:03Z`(UTC)

## 本地检查结果

- typecheck:PASS
- Vitest:7 个文件 / 168 项 PASS
- build:PASS
- `npm run check`:PASS
- Python `py_compile`(scripts/*.py):PASS

## 部署方式

- 用户本机人工执行 Serverless Devs(`@serverless-devs/s` 3.1.10,组件 fc3@0.1.22);部署脚本带 `pipefail` 与 `PIPESTATUS[0]` 真实退出码捕获,前置校验分支 / commit / Node 版本 / 工作区;退出码 0
- 未使用 CI 自动部署;凭证只在本地
- 部署提示记录:fc3 对 http-trigger 做声明式同步(`disableURLInternet: false`,公网 URL 保持开启,与部署前线上状态一致),无其他配置变化

## FC 部署标识(脱敏)

- 函数无版本 / 别名,部署到 LATEST
- 部署前:lastModifiedTime `2026-07-24T10:48:41Z`,codeChecksum `14758798687113174365`(V2-a 合并前早期版本)
- 部署后:lastModifiedTime `2026-08-01T20:41:11Z`,codeChecksum `602021559878851216`;runtime / handler / timeout / memory 与部署前一致,HTTP trigger 完整

## 回滚目标

- 类型:部署前只读导出的当前生产代码包 + 配置快照(Option C)
- 旧生产代码 SHA-256:`f320d0e739f5a79b27e1e1985a7462381ee711a9b898ed38be5059b714fbc5af`(crc64 与线上 codeChecksum `14758798687113174365` 交叉验证一致)
- 快照已从临时目录持久化到本地非云同步目录(目录 700 / 文件 600),含恢复程序说明;恢复路径与主部署同一机制(快照代码重新 `s deploy`),预计 1–2 分钟
- 本次未执行回滚

## HTTP Smoke Test

- GET `/`:200,含「野薯」「Worker 运行中」
- 飞书 challenge(url_verification):原样回显
- 错误 Token 回调:HTTP 401 `invalid token`(fail-closed,零 handler)

## 飞书原生测试

- `/add`:确认卡收到,GitHub Project 新增一项,初始 Backlog,标题正确,无重复
- `/today`:分组顺序 Doing / Next / Paused / Backlog 正确
- 正常流转:Next→Doing(start)→Paused(pause)→Doing(resume)→Done(complete,奖励文案),每次仅一次状态变化
- 旧卡片拒绝(PR-B 核心):旧 Backlog 卡片再点「排期」被服务端拒绝,状态保持 Next,无第二次 mutation
- 测试数据:1 个 `[DEPLOY-SMOKE eb20515c]` 测试任务,最终 Done(保留作验证记录,或后续人工清理)

## SHA 验证方式的限制

Worker 未通过 HTTP 直接暴露 Git commit SHA。本记录不构成「线上接口直接证明 commit」的断言;部署证据链为「main commit + 构建产物 SHA-256 + 人工部署日志 + FC 更新时间 / codeChecksum + 生产功能验证」。

## 已知剩余风险

- 见 `docs/STATUS.md`「剩余可靠性工作(Reliability Hardening 清单)」;
- FC 侧无版本 / 别名机制,回滚依赖部署前代码包快照;
- `event_id` 幂等、GraphQL 分页、WIP 并发保护、timeout/retry、错误脱敏均未实施。

## 关联留痕

- PR #2 部署验证评论:https://github.com/JettxonHo/yeshu/pull/2#issuecomment-5153379984
